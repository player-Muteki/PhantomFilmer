# 下一位 Codex 任务书：PhantomFilmer ReID 后续工作

## 你的角色

接手此项目的离线 ReID 工程化与安全验证。先复现现有结果，再补齐真实视频评估能力。除非用户另行明确授权并满足安全验收条件，不得连接或操控无人机，不得执行 ReID 真机起飞跟随。

## 当前状态

项目已完成以下工作：

1. `PersonReIDDetector`、YOLO 行人检测、Torchreid/OSNet 特征提取和检测器工厂已经接入。
2. 模型在跟随会话开始前通过 `prepare()` 加载；加载失败应发生在起飞之前。
3. OSNet 与 YOLO 权重已经下载并通过官方 SHA-256 校验，但大文件按规则不提交到 Git。
4. 完整交付包包含 PRID 2011、权重和示例图；普通 Git 克隆需按 `docs/reid_materials.md` 单独准备这些材料。
5. `data/datasets/`、`data/reid_target/`、`data/reid_test/` 和 `weights/` 已被 `.gitignore` 排除，严禁误提交人物数据或模型大文件。
6. 真实 OSNet 测试得到 AUC 0.991581；ReID 相关自动化测试 20/20 通过。
7. `config.yaml` 仍保持安全默认模式，没有自动开启 ReID。

详细证据见：

- `docs/reid_test/ReID_real_inference_report.md`
- `docs/reid_test/ReID_real_inference_results.json`
- `docs/reid_materials.md`

## 已知结论与风险

- 默认 `reid_similarity_threshold: 0.65` 很保守：PRID 同人召回率约 44.6%，异人误接收率约 0.04%。
- `0.60` 可作为离线标定起点：召回约 85.5%，异人误接收率约 1.96%，但不能直接用于飞行。
- PRID 是 64×128 左右的人物裁剪图，不是完整场景；它不能代表 Tello 摄像头中的 YOLO 检出率。
- PRID 放大图上，YOLO 默认置信度 0.45 会漏掉示例目标；0.25 能检出一部分，但该值必须用真实视频重标定。
- 当前 CPU 单人端到端测试约 46 FPS；多人、视频解码、窗口绘制会降低速度。
- Torchreid 官方安装脚本在干净环境中需要先安装 NumPy、SciPy、TensorBoard 等依赖，再使用 `--no-build-isolation`。已提供 `scripts/setup_reid_env.sh`。
- 虚拟环境里可能存在一个第三方同名 `tests` 包；运行项目测试时使用 `unittest discover -s tests`，不要使用 `tests.test_xxx` 模块路径。

## 优先任务

### P0：在干净环境复现

1. 阅读本任务书、`docs/reid_materials.md` 和测试报告。
2. 检查权重、参考图和 PRID 是否已在本地准备；缺失时按材料说明获取，不要提交到 Git。
3. 运行 `bash scripts/setup_reid_env.sh python3`。
4. 运行 `tools/reid_offline_eval.py`，确认核心指标与报告接近。
5. 运行三组 ReID 相关自动化测试，确认 20/20 通过。
6. 如安装脚本失败，修复脚本或依赖文件，并在新的干净虚拟环境再次验证。

### P1：实现真正的离线视频 dry-run

当前 `follow-dry-run` 更偏摄像头调试。增加一个明确的离线视频入口，建议支持：

```text
python main.py --mode reid-video-test --video path/to/test.mp4
```

要求：

- 绝不连接无人机、绝不创建真实适配器、绝不发送 RC 指令。
- 使用与运行时相同的 `create_detector()`、`PersonReIDDetector` 和绘制逻辑。
- 每帧记录时间戳、YOLO 候选数量、检测置信度、最佳/第二相似度、歧义状态、最终 found 状态和推理耗时。
- 输出 CSV/JSON 汇总和带标注的视频。
- 支持无 GUI 模式，适合自动化执行。
- 视频打开失败、模型失败或参考图缺失时应返回非零退出码并给出明确错误。

### P2：采集真实目标材料

需要用户提供或现场采集：

- 经本人同意的目标人物全身参考图至少 5–10 张。
- 前、后、左、右、斜侧和不同距离。
- 实际衣着与飞行测试一致。
- 一段完整场景视频，包含目标人物和至少 2 名相似衣着干扰者。
- 覆盖遮挡、逆光、转身、远近变化、目标离开和重新进入。

不要把公开 PRID 人物当作真实跟随目标。

### P3：阈值标定

基于真实视频输出：

- YOLO 检出率与置信度分布。
- ReID 正负样本相似度分布。
- 不同阈值下 TP/FN/FP/TN、召回率和误接收率。
- 多人画面中前两名差值分布，用于选择 `reid_ambiguity_margin`。
- P50/P95 延迟和实际 FPS。

阈值选择以避免误跟为第一目标。不能只追求 AUC 或平均准确率。

### P4：安全集成与测试

至少补充以下自动化测试：

- 离线视频入口不会构造或连接无人机适配器。
- ReID 模型准备失败时不会起飞。
- 多人分数接近时输出 `ambiguous=True` 且控制量为零。
- 目标丢失后立即输出零 RC；默认 `reid_temporary_lost_frames=0`。
- 预测结果不能进入目标锁定状态。
- 连续丢失按现有安全策略悬停/降落。
- 日志不保存未授权的人脸或其他敏感身份信息。

保留 ArUco 作为安全降级模式，不要删除或弱化。

## 建议修改位置

- `main.py`：增加离线视频命令行入口与参数。
- `vision/person_reid_detect.py`：必要时暴露行人置信度和各候选相似度，但保持现有检测结果协议兼容。
- `vision/detector_factory.py`：继续通过工厂创建检测器。
- `control/follow_session.py`：只复用纯视觉/控制逻辑，不允许离线入口触发真机路径。
- `tests/`：增加离线视频、日志和安全回归测试。
- `README.md`：补充可复现命令、参数说明、数据隐私和安全限制。

## 验收标准

进入下一阶段前至少满足：

1. 干净环境一条脚本完成安装，OSNet/YOLO 权重均从本地加载，不发生隐式下载。
2. 离线视频模式可重复执行，输出结构化指标和标注视频。
3. 所有现有测试通过，新增安全测试通过。
4. 真实验证集没有出现任何异人被持续接受和跟随的情况。
5. 目标丢失或歧义时控制输出始终为零。
6. 实际视频 P95 推理延迟低于 100 ms；若达不到，先优化或降低控制频率。
7. 用户审阅离线报告并明确授权后，才可讨论地面静态真机测试。

## 禁止事项

- 不得因 PRID AUC 较高就声称真机可靠。
- 不得直接把示例阈值写入真机生产配置并起飞。
- 不得下载来源不明的模型权重或人物数据。
- 不得在未获本人同意时采集、保留或传播真实人物身份材料。
- 不得绕过 `SafetyManager`、起飞确认或模型预加载检查。
- 不得自动开始任何真实飞行测试。

## 交付物

下一位 Codex 完成任务后应交付：

- 代码变更与新增测试。
- 干净环境安装验证记录。
- 真实离线视频测试报告与原始 JSON/CSV 指标。
- 阈值建议及其误接收/漏检依据。
- 明确的“是否可以进入地面静态真机测试”结论；若证据不足，必须写明不可进入。
