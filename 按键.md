# PhantomFilmer 按键说明

所有按键都必须在无人机摄像头窗口处于焦点时使用。`Q` 和 `E` 在任何控制状态下
都有最高优先级。

## 起飞后的首次模式选择

无人机到达基础悬停高度并稳定后，保持悬停等待选择：

| 按键 | 功能 |
| --- | --- |
| `A` | 进入普通自动跟随 |
| `S` | 进入侧向跟随，自动选择较近的 `90°/270°` |
| `F` | 当前界面会请求前向跟随，但生命周期处理器尚未接入；请勿作为操作路径 |
| `M` | 进入手动控制 |
| `Q` | 正常停止并降落 |
| `E` | 急停、清零并降落 |

当前进入前向跟随的可靠路径是先按 `A` 进入普通自动，再按 `3` 切换。

## 自动飞行中的模式切换

自动飞行时只使用数字键切换模式，避免与手动飞行方向键冲突：

| 按键 | 功能 |
| --- | --- |
| `1` | 切换到普通自动跟随，并恢复普通模式避障 |
| `2` | 切换到侧向跟随，重新采样并选择 `90°/270°` |
| `3` | 切换到前向跟随，重新采样并锁定 `180°` |
| `M` | 进入手动控制 |
| `Q` | 正常停止并降落 |
| `E` | 急停、清零并降落 |

按下 `1/2/3` 后，无人机会先清零并进入 `MODE_SWITCHING`。系统清除旧模式状态，
连续确认目标身份后才启动新模式。如果人物暂时丢失，则执行目标搜索；找回并确认
后再进入目标模式。按下当前已经运行的模式键不会重复重置。

## 手动控制

| 按键 | 功能 |
| --- | --- |
| `W` | 前进 |
| `S` | 后退 |
| `A` | 向左横移 |
| `D` | 向右横移 |
| `R` | 上升 |
| `F` | 下降 |
| `J` | 向左偏航 |
| `L` | 向右偏航 |
| `空格` | 立即悬停 |
| `M` | 退出手动，重新确认人物后恢复进入手动前的自动模式 |
| `Q` | 正常停止并降落 |
| `E` | 急停、清零并降落 |

手动状态下 `1/2/3` 不切换模式。必须先按 `M` 退出手动，再使用数字键切换，
因此 `A/S/F` 始终只表示手动方向，不会误触发自动模式。

## 模式说明

- 普通跟随：使用原普通跟随与避障逻辑。
- 侧向跟随：目标角度为较近的 `90°` 或 `270°`，绕过避障。
- 前向跟随：目标角度固定为 `180°`，绕过避障。
- 侧向和前向模式丢失人物后复用普通目标搜索；重新找到人物后返回原模式。
- 侧向和前向模式的短暂漏检先悬停确认；持续丢失达到默认 `0.5` 秒后才进入搜索，避免状态频繁跳变。

## 安全提示

- 前向和侧向模式不使用避障，测试区域必须宽阔，飞机后方、侧方和上方必须净空。
- 模式切换期间不要连续敲击多个模式键，应等待画面显示切换完成。

## Electron 桌面端按键（2026-08 起）

桌面端与 CLI OpenCV 窗口共用同一套语义按键通道，差异如下：

| 按键 | 桌面端行为 | 与 CLI 的差异 |
|------|-----------|--------------|
| `1/2/3`、`M` | 切换普通/侧向/前向/手动（任务运行中） | 一致 |
| `P` | 暂停/继续任务 | 桌面端全模式可用，且有对应按钮 |
| `Q` | 停止并降落 | 桌面端需 4 秒内按两次（与按钮一致） |
| `E` | 急停 | 桌面端需 4 秒内按两次（与按钮一致） |
| `W/S/A/D/R/F/J/L`、空格 | 手动方向与悬停（手动接管后） | 桌面端可在 HUD 中关闭键盘控制；输入框聚焦时飞行键失效 |

视频画面按原始比例放大铺满（上下边缘可能轻微裁切）；控制板为视频底部半透明 HUD 叠加。
- `Q` 是正常降落，`E` 是急停降落；任何异常情况下优先保证人员安全。
