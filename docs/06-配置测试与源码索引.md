# 06 · 配置测试与源码索引

> [返回总入口](README.md) · 上一篇：[05-安全机制与硬件边界](05-安全机制与硬件边界.md) · 下一篇：无
>
> 本文是配置与测试的**唯一事实源**：配置来源优先级、当前 YAML 值与代码 fallback、运行期覆盖与兼容/闲置项、依赖与本地数据、测试覆盖形态、质量门禁缺口，以及完整源码索引。

## 1. 配置来源优先级

`app/config.py`：

| 层 | 机制 | 代码 |
|----|------|------|
| 1. 配置 YAML | `load_config()` 用 PyYAML（无 PyYAML 时回退解析器） | [app/config.py:11](../app/config.py#L11) |
| 2. CLI 参数 | `parse_args` | [main.py:44](../main.py#L44) |
| 3. 运行期内存覆盖 | `load_runtime_config(obstacle_enabled)` 覆盖 `obstacle.enabled` | [app/config.py:34](../app/config.py#L34) |
| 4. ReID 派生覆盖 | `build_reid_runtime_config()` 覆盖 `vision.detector_type` 与距离带 | [vision/reid_enrollment.py:51](../vision/reid_enrollment.py#L51) |
| 5. 环境变量 | `LLM_API_KEY`（LLM 客户端） | [console/llm_client.py:30](../console/llm_client.py#L30) |
| 6. 代码 fallback | 各构造器 `default=` 参数 | 见各节 |

回退解析器 `_load_config_without_yaml`（[app/config.py:69](../app/config.py#L69)）：只支持顶层标量 + `vision/obstacle/target_search` 三个扁平块；遇到其他嵌套块抛 `ValueError` 而非静默丢弃。

## 2. 配置当前值与消费者

### 2.1 安全与高度（顶部键）

| 键 | 当前值 | 消费者 |
|----|--------|--------|
| `min_battery_takeoff` | 20 | `SafetyConfig`（[drone/safety.py:24](../drone/safety.py#L24)） |
| `low_battery_land` | 5 | `SafetyConfig`（[drone/safety.py:25](../drone/safety.py#L25)） |
| `max_height_cm` | 220 | `SafetyConfig` / `FollowSession` 上限 |
| `min_height_cm` | 60 | `SafetyConfig`（供搜索/爬升范围） |
| `base_hover_height_cm` | 150 | `_reach_base_hover_height`（[control/follow_session.py:524](../control/follow_session.py#L524)） |
| `base_hover_height_tolerance_cm` | 5 | 同上（[control/follow_session.py:541](../control/follow_session.py#L541)） |
| `base_hover_vertical_speed` | 20 | 同上（[control/follow_session.py:544](../control/follow_session.py#L544)） |
| `base_hover_stable_readings` | 3 | 同上（[control/follow_session.py:551](../control/follow_session.py#L551)） |
| `takeoff_height_verify_enabled` | **false** | `HeightVerifyHandler` 分支；关闭则跳过 |
| `takeoff_height_verify_timeout_seconds` | 5 | 仅启用时生效 |
| `takeoff_height_min_cm` / `stable_readings` / `sample_interval` | 20 / 3 / 0.2 | 仅启用时生效 |
| `height_filter_window` | 5 | `_read_height` 中位数窗口（[control/follow_session.py:106](../control/follow_session.py#L106)） |
| `height_max_valid_cm` | 500 | 高度有效性上界 |
| `height_failure_limit` | 10 | `_loop` 高度失败计数 |
| `max_rc_speed` | 35 | `SafetyManager.limit_rc_command` |
| `camera_width` / `camera_height` | 640 / 480 | CameraStream / Fake |

### 2.2 视觉

| 键 | 当前值 | 消费者 |
|----|--------|--------|
| `vision.detector_type` | `aruco` | `create_detector`（[vision/detector_factory.py:35](../vision/detector_factory.py#L35)） |
| `vision.aruco_dictionary` | `DICT_4X4_50` | `ArucoTargetDetector` |
| `vision.target_marker_id` | 23 | 同上 |
| `vision.smoothing_alpha` | 0.30 | 同上 |
| `vision.temporary_lost_frames` | 0 | 同上 |
| `vision.min_marker_area` | 300 | 同上 |
| `vision.reference_images` | `data/reid_target/front.jpg,side.jpg` | `PersonReIDDetector` |
| `vision.person_detector_model` | `weights/yolov8n.pt` | YOLO |
| `vision.person_detection_confidence` | 0.45 | 同上 |
| `vision.reid_model_name` / `path` | `osnet_x0_25` / `weights/osnet_x0_25_msmt17.pth` | Torchreid |
| `vision.reid_device` | `cpu` | 同上 |
| `vision.reid_similarity_threshold` | 0.65 | ReID 接受阈值 |
| `vision.reid_ambiguity_margin` | 0.05 | ReID 歧义判定 |
| `vision.reid_temporary_lost_frames` | 0 | ReID 丢失容忍 |
| `detector_min_area` / `detector_min_area_ratio` | 500 / 0.002 | `TargetDetector` |

### 2.3 跟随控制

| 键 | 当前值 | 消费者 |
|----|--------|--------|
| `horizontal_dead_zone_ratio` | 0.08 | `FollowController` |
| `yaw_kp` | 160 | 同上 |
| `minimum_yaw_speed` | 15 | 同上 |
| `maximum_yaw_speed` | 25 | 同上 |
| `target_area_ratio_min` / `max` | 0.030 / 0.080 | 前后距离带 |
| `reid_target_area_ratio_min` / `max` | 0.22 / 0.32 | ReID 运行期覆盖（不直接消费） |
| `maximum_forward_speed` | 25 | 同上 |
| `forward_speed_while_aligning` | 16 | 对准减速 |
| `vertical_dead_zone_ratio` | 0.10 | 同上 |
| `vertical_speed` | 20 | 同上 |
| `target_lock_stable_frames` | 15 | LOCKED 进入 |
| `target_lock_exit_area_ratio_min` / `max` | 0.025 / 0.085 | LOCKED 退出（滞回） |
| `reid_target_lock_exit_area_ratio_min` / `max` | 0.20 / 0.34 | ReID 覆盖 |
| `target_lock_exit_horizontal_dead_zone_ratio` | 0.12 | LOCKED 退出 |
| `target_lock_exit_vertical_dead_zone_ratio` | 0.15 | 同上 |
| `control_interval` | 0.05 | `_read_control_interval`（[app/config.py:159](../app/config.py#L159)） |
| `min_control_hz` | 8 | 控制率告警 |
| `frame_failure_limit` | 30 | 失败收敛 |
| `display_console_camera` | true | 调试显示 |

### 2.4 避障

| 键 | 当前值 | 消费者 |
|----|--------|--------|
| `obstacle.enabled` | **false** | `build_obstacle_modules`（[app/builder.py:41](../app/builder.py#L41)） |
| `obstacle.front_tof_enabled` | true | `_fuse_front_tof` 开关（[control/motion_arbiter.py:303](../control/motion_arbiter.py#L303)） |
| `obstacle.front_tof_blocked_distance_cm` | 60 | BLOCKED 阈值 |
| `obstacle.front_tof_clear_distance_cm` | 70 | **当前未消费**（初始化 `clearance_distance_cm`，无读取路径） |
| `obstacle.front_tof_poll_interval_seconds` | 0.2 | `FrontToFMonitor` |
| `obstacle.front_tof_max_age_seconds` | 0.8 | snapshot 新鲜度 |
| `obstacle.center_loss_forward_enabled` | true | 中心丢失前探开关 |
| `obstacle.center_loss_forward_speed` | 25 | 前探速度 |
| `obstacle.center_loss_history_frames` | 3 | 前探触发帧数 |
| `obstacle.center_loss_edge_margin_ratio` | 0.05 | 边距判定 |
| `obstacle.avoidance_lateral_speed` | 20 | 侧移速度 |
| `obstacle.bypass_lateral_direction` | right | 侧移方向 |
| `obstacle.bypass_lateral_distance_cm` | 100 | 侧移估算距离 |
| `obstacle.bypass_max_sidestep_seconds` | 10 | 侧移超时 |
| `obstacle.lost_tof_sample_count` | 5 | 丢失调查样本数 |
| `obstacle.lost_tof_failure_limit` | 5 | ToF 失败降落阈值 |
| `obstacle.post_bypass_turn_degrees` | 90 | 左转角度 |
| `obstacle.post_bypass_turn_speed` | 12 | 左转速度 |
| `obstacle.post_bypass_turn_timeout_seconds` | 15 | 左转超时 |
| `obstacle.recovery_clear_frames` | 10 | 恢复确认 |
| `obstacle.detect_confirm_frames` | 3 | 阻挡确认 |
| `obstacle.clear_confirm_frames` | 5 | 恢复确认 |
| `obstacle.timeout_action` | land | FAILSAFE 动作 |
| `obstacle.log_enabled` | true | 事件日志开关 |
| `obstacle.log_dir` | `logs/avoidance` | 日志目录 |
| `obstacle.log_every_n_frames` | 2 | 日志节流 |
| `obstacle.detect_every_n_frames` | 1 | 探测节流 |

### 2.5 目标搜索

| 键 | 当前值 | 消费者 |
|----|--------|--------|
| `target_search.enabled` | true | `TargetSearchController` 开关 |
| `target_search.hold_seconds` | 1.0 | 丢失悬停 |
| `target_search.reacquire_frames` | 5 | 搜索后重新确认帧数 |
| `target_search.last_direction_yaw_speed` | 25 | 最后方向偏航 |
| `target_search.yaw_speed` | 30 | 层扫描偏航 |
| `target_search.vertical_speed` | 20 | 层间升降 |
| `target_search.last_direction_seconds` | 2.0 | 最后方向时长 |
| `target_search.full_rotation_degrees` | 360 | 层完整旋转 |
| `target_search.full_rotation_fallback_seconds` | 12.0 | 无遥测回退 |
| `target_search.height_step_cm` | 20 | 层高步进 |
| `target_search.min_height_cm` / `max_height_cm` | 80 / 200 | 搜索高度带 |
| `target_search.close_recovery_enabled` | true | 过近后退开关 |
| `target_search.close_area_ratio` / `close_very_area_ratio` | 0.32 / 0.37 | 过近阈值 |
| `target_search.close_backward_speed` | 35 | 后退速度 |
| `target_search.close_pulse_seconds` | 1.5 | 后退时长 |
| `target_search.close_pause_seconds` | 0.50 | 停顿时长 |
| `target_search.close_max_attempts` | 2 | 后退次数 |
| `target_search.edge_margin_ratio` | 0.03 | 触边判定 |

### 2.6 控制台 / LLM / Fake / 丢失

| 键 | 当前值 | 消费者 |
|----|--------|--------|
| `lost_target_hover_seconds` / `lost_target_land_seconds` | 3 / 8 | `SafetyConfig` |
| `llm_enabled` | true | `LLMClient.enabled` |
| `llm_base_url` | `https://api.deepseek.com/chat/completions` | `LLMClient` |
| `llm_model` | `deepseek-chat` | 同上 |
| `llm_timeout_seconds` | 8 | 同上 |
| `task_stop_timeout_seconds` | 10 | Console 停止超时 |
| `console_window_name` | `PhantomFilmer Console Follow` | Console 会话窗口 |
| `fake_target_speed` | 3 | Fake 目标速度 |
| `fake_target_lost_interval_seconds` / `duration_seconds` | 12 / 2 | Fake 丢失周期 |
| `display_console_camera` | true | 显示开关 |

## 3. 运行期覆盖与兼容/闲置项

- **运行期覆盖**：`obstacle.enabled`（避障询问，[app/config.py:43](../app/config.py#L43)）；`vision.detector_type=person_reid` 与 ReID 距离带（`reid_*` → 共享键，[vision/reid_enrollment.py:78](../vision/reid_enrollment.py#L78)）。
- **兼容但当前未消费**：
  - `--lock-frames`（[main.py:112](../main.py#L112) → [app/modes.py:425](../app/modes.py#L425) `del`）。
  - `reid_lock_stable_frames`、`reid_lock_timeout_seconds`、`reid_confirmation_timeout_seconds`（[config.yaml:47-49](../config.yaml#L47)）：旧地面锁定流程的兼容参数，当前 `reid-demo` 直接由终端 `y` 授权起飞。
  - `obstacle.front_tof_clear_distance_cm`：初始化给 `clearance_distance_cm` 但主路径未读。
  - Planner 构造器中的 legacy 参数（`avoidance_yaw_speed` 等）：接受后 `del`（[control/obstacle_avoidance.py:82](../control/obstacle_avoidance.py#L82)）。
- **`takeoff_height_verify_*`**：默认关闭（[config.yaml:13](../config.yaml#L13)），启用后才生效。

## 4. 依赖与本地数据

| 文件 | 用途 |
|------|------|
| `requirements.txt` | `djitellopy==2.5.0`、`opencv-python` / `opencv-contrib-python` 同版本 4.14.0.94、`numpy`、`PyYAML`、`Pillow` |
| `requirements-dev.txt` | + `pytest==9.1.1` |
| `requirements-reid.txt` / `requirements-reid-bootstrap.txt` | ReID 依赖（YOLO、torchreid 等） |
| `scripts/setup_reid_env.sh` | 建 `.venv-reid`，从 git 安装 `deep-person-reid@f8cd150…` |
| `tools/generate_aruco_marker.py` | 生成目标 ArUco 标记到 `docs/aruco_marker_23.png` |
| `tools/reid_offline_eval.py` | PRID 2011 离线评估（只测裁剪人物 ReID，不测飞控） |

本地数据（全部被 Git 忽略，[.gitignore:11-16](../.gitignore#L11)）：`weights/`、`data/reid_target/`、`data/reid_profiles/`、`data/reid_test/`、`data/datasets/`、`logs/`。

## 5. 测试覆盖形态

- **23 个测试模块**，**316 个 `test_*` 函数**（`grep -rc "def test_"` 实测）。
- `pytest.ini`：`testpaths = tests`、`pythonpath = .`。
- 覆盖领域（按模块）：
  - 状态机/确定性：`test_kernel_phases`、`test_follow_session_compat`、`test_follow_session_detector_reset`、`test_feature_adapters`、`test_target_search`、`test_obstacle_avoidance`、`test_motion_arbiter`、`test_fixed_demo`。
  - 适配器与硬件：`test_tello_adapter`、`test_front_tof`。
  - 检测器：`test_aruco_detector`、`test_detector_factory`、`test_person_reid_detector`、`test_follow_aruco_integration`。
  - ReID 档案：`test_reid_enrollment`、`test_reid_profiles`。
  - Console：`test_console_command_parser`、`test_console_controller_llm`、`test_console_follow_session`。
  - 配置/CLI：`test_config_fallback`、`test_cli_obstacle_prompt`、`test_imports`。
  - 避障优先级：`test_follow_session_obstacle_priority`。

> **实测结果（2026-08-17）**：`.venv/bin/python -m pytest tests/ -q` → **316 passed，0 failed，0 skipped**（另 17 subtests passed），用时约 20 s。本结果只代表当前工作树在本次环境下的状态，改动后需重新运行。

### 质量门禁缺口（静态事实）

- 无 CI 配置、无 coverage 门槛、无 lint（ruff/flake8）配置、无类型检查（mypy/pyright）配置。
- 无硬件在环测试、无闭环动力学/风模型模拟、无真实 ToF 噪声模型。
- 测试依赖 patch `control.follow_session.sleep/monotonic`（[control/kernel/session.py:6](../control/kernel/session.py#L6)），说明存在时间类依赖硬编码。

## 6. 完整源码索引

```text
main.py                                  入口：parse_args / main
app/
  config.py                              配置加载 / 回退解析 / 覆盖
  builder.py                             依赖装配（安全/避障/适配器/Console）
  modes.py                               14 种模式 run_*
control/
  follow_session.py                      FollowSession 兼容门面（1335 行）
  follow_control.py                      FollowController（RC 转换 + LOCKED）
  target_search.py                       TargetSearchController（三层搜索）
  motion_arbiter.py                      MotionArbiter + _JsonlEventWriter
  obstacle_avoidance.py                  ObstacleAvoidancePlanner
  fixed_demo.py                          FixedDemoManeuver
  features/                              Feature-SDK 适配器（follow/obstacle/search/safety）
  kernel/
    session.py                           KernelSession
    phases.py                            KernelPhase 枚举 / PhaseHandler 协议
    arbitration.py                       ArbitrationEngine（配方 1-6）
    features.py                          FeatureProposal / ArbitrationContext / MotionFeature
    phase_handlers/                      PRE_FLIGHT→…→FOLLOW 处理器
console/
  commands.py                            ConsoleCommand 六值白名单
  command_parser.py                      本地规则优先解析
  llm_client.py                          OpenAI 兼容分类客户端
  tools.py                               ConsoleTools 任务工具
  console_controller.py                  ConsoleController 交互循环
drone/
  drone_adapter.py                       DroneAdapter 抽象
  tello_adapter.py                       TelloDroneAdapter（唯一 djitellopy 导入）
  fake_adapter.py                        FakeDroneAdapter
  safety.py                              SafetyConfig / SafetyManager
  front_tof.py                           FrontToFMonitor
vision/
  detector_protocol.py                   DetectorProtocol
  detector_factory.py                    create_detector
  target_detect.py                       TargetDetector（红）
  aruco_detect.py                        ArucoTargetDetector
  person_reid_detect.py                  PersonReIDDetector
  reid_enrollment.py                     录入 + build_reid_runtime_config + TargetLockTracker
  reid_profiles.py                       档案 save/load/校验/原子写
  obstacle_detect.py                     DistanceOnlyObstacleDetector
  camera.py                              CameraStream
tools/  scripts/  tests/  config.yaml  requirements*.txt  pytest.ini  .gitignore
```

## 7. 代码依据与不保证

- 代码依据：`app/config.py`（[app/config.py:11](../app/config.py#L11)）、`config.yaml`（[config.yaml:1](../config.yaml#L1)）、`pytest.ini`、`.gitignore`、各构造器 `from_config`。
- 不保证：测试于 2026-08-17 全绿（316 passed），但未覆盖真机/真实 Wi-Fi/真实 ToF；未验证所有配置键在运行时都被正确消费（表中“当前未消费”项是静态核对结果）；质量门禁缺口说明当前仓库无 CI/coverage/lint/type 门禁。
